from cs50 import get_int

while True:
    number = input("Введіть ціле число: ")
    if not number.isdigit():
            number
    else:
        if 13<int(number)<17:
            for i in number:
                print(number)
            break
        else:
             number
